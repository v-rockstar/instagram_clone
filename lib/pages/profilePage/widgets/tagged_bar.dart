import 'package:flutter/material.dart';

class TaggedBar extends StatelessWidget {
  const TaggedBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
